# AI Skin v3 — 공통 의존성 (서버·CLI·GUI 공통)
# 설치: pip install -r requirements-core.txt

# Core ML/DL
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0

# Image processing (headless — GUI 없는 서버/CLI 환경)
opencv-python-headless>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
scikit-image>=0.21.0
scipy>=1.10.0

# Gemini AI
google-generativeai>=0.3.0

# Other LLM providers (for testing)
openai>=1.0.0
anthropic>=0.18.0

# CodeFormer dependencies
addict
future
lmdb
requests
lpips
gdown

# RestoreFormer++ dependencies
facexlib==0.2.5
ffmpeg-python==0.2.0
omegaconf>=2.1.1,<3
pytorch-lightning>=2.0.0
basicsr==1.4.2
realesrgan==0.3.0

# Web API (FastAPI server)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Database (Supabase sync)
supabase>=2.0.0

# Security & Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9

# Retry & Resilience
tenacity>=8.2.0

# CLI Tools
click>=8.1.0

# Other utilities
pyyaml>=6.0
tqdm>=4.65.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
psutil>=5.9.0
watchdog>=3.0.0
python-barcode>=0.15.0
